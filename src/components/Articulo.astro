---
const { numero, titulo, texto } = Astro.props;
---
<article class="border w-full h-full rounded flex flex-col px-5 py-2 md:max-w-md">
    <header class="flex w-full justify-between cursor-pointer">
        <h2 class="xs:text-xl font-light lg:text-2xl">{numero} <strong id="atributo" class="font-black px-2" style="color: #00f0ff">{titulo}</strong></h2>
        <img id="botonFlecha" src="/arrow.svg" alt="" class="rotate-180 cursor-pointer">
    </header>
    <p class="hidden xs:text-sm py-3 lg:text-xl">{texto}</p>
</article>


